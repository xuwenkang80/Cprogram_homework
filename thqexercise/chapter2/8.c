//8.c程序用于输出1900-2000年之间的闰年的年份（此程序暂时不可用，请转至DX版本）

#include <stdio.h>

int year[120];

int main()
{
	for(int i=0;i<100;i++)
	{
		if(((i+1900)%4==0&&(i+1900)%100!=0)||((i+1900)%100==0&&(i+1900)%400==0)){printf("%d是一个闰年！\n",i+1900);}
	}
	return 0;
}
